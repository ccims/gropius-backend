/**
 * Configuration for the Gropius backend modules that are deployed as executable
 * Spring Boot applications.
 */
plugins {
    id("gropius.kotlin-conventions")
    id("org.springframework.boot")
}
